# Orchestration — open decisions

**Version**: 0.1 · **Last Updated**: 2026-09-17 · **Status**: 🔴 DA REVISIONARE

Source of every row below: `kajenn-meta/verification/20_spa/020_orchestration.md`.

Rows of that report whose verdict is `DIVERGE` or `SILENT`. They are neither ratified nor
dropped: each one is a decision for the owner. `decisions.md` of this entry keeps the
`CONVERGE` rows only.

## Rows

| # | Entry in decisions.md | Implementation (code file:line, test) | Docstring (file:line, text) | Handoff/finaldoc (file, date, text) | Transcript (uuid, date, role, text) | Verdict |
|---|---|---|---|---|---|---|
| 6 | "a group **without** the factory spawns workers the ordinary way and has no template at all — a composition difference, not a flag" | `group_handler.py:454-456` `self.template = (None if engine_factory is None else TemplateConnector(…))`; `worker_handler.py:578-581` `template = self.group_handler.template` / `if template is None: return self.spawn_process()` — the choice is on the presence of the template, and `grep -rn worker_mode src/ tests/` returns 0 lines · `tests/spa/test_config_commander.py::test_a_group_that_declares_no_engine_factory_owns_no_template` ("assert group.template is None") | `group_handler.py:452-453` "#: This group's template, when a factory was declared for it: the process its workers are forked from. None means its workers are spawned instead."; `spa_app.py:302-303` "Omitted, the group spawns its workers the ordinary way and has no template at all." | SILENT. `temp/contratto_fork_template_2026-08-24.md` does not describe the group without a factory; §12 point 2 leaves open only the neighbouring question: "Il gruppo ripiega sullo spawn quando il template è giù … o rifiuta di crescere finché il template non è su?" | `6d2cade3-…jsonl`, uuid `8154ed8b-7956-4a68-8fb0-321d39c3e705` (2026-08-24 12:14, user): "io pensavo ad u n parametro del ngrup dove ho max workers e worker mode spawn o fork"; uuid `517cbb16-43dc-42a3-9bb0-9fee643ce9b1` (2026-08-24 12:13, user): "intendo dire cge potrebbe essere una proprieta deol group se fare spawn o fork ?" | DIVERGE |
| 7 | friction — "F48/F49: a fold that fails in the parent no longer kills the worker, but the partially applied envelope has no escalation/resync yet; the two decisions are cited by code and commits and still missing from the F1–F47 register." | `worker_connector.py:411-419` `try: self.worker_handler.read_envelope(info)` / `except Exception: self._logger.exception("The fold refused the envelope %s from the child on %s", …)` — nothing is done to the child and no escalation follows; `grep -rn kill_worker src/ tests/` returns 0 lines, so the verb is gone. No test in `tests/` exercises a fold that raises: `grep -rn "def test_.*fold" tests/` returns no such test. | `worker_connector.py:403-409` "A fold that raises is a fault of THIS side … the exception is logged with its stack and nothing is done to the child: the orchestration neither corrects nor masks, and the worker is not answerable for it. The events of that envelope stay half applied, which is the declared price until the escalation of F48 exists." | `temp/interview_handler_2026-08-15.md` (entries dated 2026-08-19), line 2234: "### F48 — Il worker è un client della porta pubblica: identità, token, endpoint (dettato, 2026-08-19)"; line 2274: "### F49 — L'orchestrazione non corregge e non maschera: `kill_worker` abolito (dettato, 2026-08-19)"; line 2305: "quel ramo vorrà `notify_sysop` (F48) e fino ad allora resta NON gestito." | `277be791-7ca7-48fb-b185-e5c81ac7aafd.jsonl`, uuid `0ab1b2ed-d6d6-4be5-9842-fe7f6f8bcb80` (2026-08-19, assistant): "**Il registro** (`temp/interview_handler_2026-08-15.md`, l'autorità) — nuova voce **F48**, dettata oggi"; uuid `6fc61890-dc1d-4499-b7ec-3678c6056a4f` (2026-08-19, assistant): "Vuoi che questa decisione finisca nel registro come F49, come abbiamo fatto per F48?"; uuid `8a0dd246-02a0-4766-827c-d611cd079137` (2026-08-19, assistant): "Registrati F48 … e F49 …" — taken up in `temp/interview_handler_2026-08-15.md`, so a valid source | DIVERGE |

## Divergences

### #6
- decisions.md says: "a group **without** the factory spawns workers the ordinary way and has no template at all — a composition difference, not a flag".
- the implementation says the same thing: `group_handler.py:454-456` `self.template = (None if engine_factory is None else TemplateConnector(…))`, and `worker_handler.py:578-581` chooses on the presence of the template — `template = self.group_handler.template` / `if template is None: return self.spawn_process()`. No mode parameter exists: `grep -rn worker_mode src/ tests/` returns 0 lines. Fixed by `tests/spa/test_config_commander.py::test_a_group_that_declares_no_engine_factory_owns_no_template` ("assert group.template is None").
- the docstring says the same thing. `group_handler.py:452-453` "None means its workers are spawned instead"; `spa_app.py:302-303` "Omitted, the group spawns its workers the ordinary way and has no template at all".
- the handoff of 2026-08-24 says: SILENT on the group without a factory.
- the transcript `6d2cade3-6cf5-4556-8f6f-cfbb8fe49040.jsonl` of 2026-08-24 says, uuid `8154ed8b-7956-4a68-8fb0-321d39c3e705` (12:14, user): "io pensavo ad u n parametro del ngrup dove ho max workers e worker mode spawn o fork". In the same round, uuid `cf5852e1-325f-45da-bc2c-158a683123b4` (12:15, user): "ma non potremmo avere due classi worker handler uno per spawn e uno per fork ?". The following session of the same day, `3cb97bad-4727-4a05-beee-cb5a1c53dfa3.jsonl` uuid `8162e996-1b6a-44f2-b510-5706fb8e3136` (14:22, user), ratifies the synchronous template without naming either the `worker_mode` parameter or the "not a flag" rule.

### #7
- decisions.md says: "the two decisions are cited by code and commits and still missing from the F1–F47 register."
- the implementation at `worker_connector.py:411-419` does what the friction's first half describes — it logs and leaves the child alone — and carries no escalation and no resync: after the `except Exception:` branch the method returns the frame unchanged (`worker_connector.py:420-421`). `kill_worker` is absent from `src/` and from `tests/`. No test exercises a fold that raises, so the half-applied envelope is unproven by any executable contract.
- the docstring says: `worker_connector.py:409` cites F48 as the debt — "until the escalation of F48 exists" — and says nothing about the register.
- the register of 2026-08-19 (`temp/interview_handler_2026-08-15.md`) holds both: line 2234 "### F48 — Il worker è un client della porta pubblica: identità, token, endpoint (dettato, 2026-08-19)" and line 2274 "### F49 — L'orchestrazione non corregge e non maschera: `kill_worker` abolito (dettato, 2026-08-19)".
- the transcript `277be791-7ca7-48fb-b185-e5c81ac7aafd.jsonl` of 2026-08-19 says, uuid `e2ea428a-01c7-458b-bf38-63b050e16ad9` (assistant): "I registri (`temp/`) restano fuori dal commit: quella cartella è in `.gitignore`, quindi F48 e F49 vivono solo in locale."
- second divergence inside the same row: `decisions.md` describes F48 as the missing escalation after a partially applied fold; the register titles F48 "Il worker è un client della porta pubblica: identità, token, endpoint" and puts the price of the half-applied fold in F49 (line 2274, point 3: "Prezzo accettato: gli annunci di quella busta restano applicati a metà"). The transcript `f8c16f5f-78f5-495a-9eef-1d6bbcfaf77d.jsonl` of 2026-08-23, uuid `a2b25d54-7122-4d5e-94dd-67df59bee5f0` (assistant), uses a third sense: "**F49** (commit `2063616`, 2026-08-19): un fold che alza è un difetto del lato padre … E **F48** è l'escalation che non esiste ancora, citata da `worker_connector.py:382`".
